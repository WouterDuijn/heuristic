# heuristic

To run the algorithm follow the following steps in Main.java:
- Set the INPUT_PATH constant to the inp folder of the repository
- Set the ALGORITHM (choose between the options in the above enum)
- Set the NR_RUNS & TOTAL_ITERATIONS.
- Press execute:)